namespace Tamga.Sdk.Tests;

// STUB — mirrors src/Tamga.Sdk/Client.cs. No real tests yet; Client.cs has
// no logic to exercise. See docs/plans/tamga-dotnet.plan.md §B–K for the
// intended test coverage (one test class per endpoint group, using
// Support/MockHttpMessageHandler.cs — not yet created — for no-live-network
// HTTP mocking), including client-side ttl validation (§F) and the
// ActivateMachineAsync over-limit delete path (§G).
